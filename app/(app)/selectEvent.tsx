import React from 'react';
import SelectEventScreen from '../src/screens/SelectEventScreen';

/**
 * Route pour la sélection d'un événement
 * Accessible via /selectEvent
 * Utilisée depuis le formulaire d'ajout de performance
 */
export default function SelectEvent() {
  return <SelectEventScreen />;
}
