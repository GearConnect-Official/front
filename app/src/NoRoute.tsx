// This component is used as a default export for non-route files
// to prevent Expo Router warnings
import React from 'react';

const NoRoute: React.FC = () => {
  return null;
};

export default NoRoute;

