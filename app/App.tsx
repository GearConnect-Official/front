import { Redirect } from 'expo-router';

// Ce fichier n'est plus nécessaire avec expo-router
// Il redirige vers la nouvelle structure de routage

export default function App() {
  return <Redirect href="/" />;
}
