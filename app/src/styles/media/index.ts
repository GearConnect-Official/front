// Exports for media styles
export { mediaStyles } from './mediaStyles';
export { default as imageViewerStyles } from './imageViewerStyles';

// Default export to prevent Expo Router warnings
export { default } from '../../NoRoute'; 